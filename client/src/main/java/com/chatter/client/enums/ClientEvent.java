package com.chatter.client.enums;

import com.chatter.core.event.listener.Event;

public enum ClientEvent implements Event {
	STARTED_APPLICATION, LOGGED_IN_ACCOUNT, OUTGOING_MESSAGE, INCOMING_MESSAGE, REGISTER

}
